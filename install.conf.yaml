- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: 
    ~/.vim: 
    ~/.vimrc: 
    ~/.zshrc: 
    ~/.shell:
    ~/.dotfiles: ''

- create:
    - ~/.vim/undo-history

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
