# ls aliases
alias ll='ls -lahF'
alias la='ls -A'
alias l='ls'

# Aliases to protect against overwriting
alias cp='cp -i'
alias mv='mv -i'

# Python alias to homebrew formula
alias python=/usr/bin/python3
